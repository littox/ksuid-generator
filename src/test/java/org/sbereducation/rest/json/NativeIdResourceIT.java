package org.sbereducation.rest.json;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeIdResourceIT extends IdResourceTest {

    // Execute the same tests but in native mode.
}